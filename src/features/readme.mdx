# OAuth 구현

두 가지 flow가 있다.

1. implicit flow
2. PKCE flow

## 1. implicit flow

로그인을 브라우저단에서 실행함.
server단이 필요 없음(supabase에서 처리함). 브라우저단에서 처리함.

클라이언트에 엑세스 토큰, 리프레쉬 토큰이 저장됨.
보안상 이슈가 있음

## 2. PKCE flow

앱이 필요한 액세스 및 새로 고침 토큰을 서버에서 처리하여 브라우저로 넘긴다
